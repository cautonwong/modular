#include "edge/module.h"
int main(void){
    /* Product-generated builds construct the instance table from ModuleIR. */
    for(;;){
        /* edge_event_router_dispatch(); */
        /* edge_module_poll_all(&manager); */
        /* other bounded foreground work */
    }
    return 0;
}
