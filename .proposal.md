我直接扎入关键问题，逐个剖析。我已读完全文。这是一份非常详尽的架构设计文档（约1.6万行），包含85条冻结决策（D1–D85）。我来直接进入你的三个问题的分析。

---

## 方案完整性评估

### ✅ 已闭合的部分（强项）

1. **顶层架构清晰**
   - 三轴SPL模型（sys × board × app）完整
   - D1–D85全部拍板，无悬空决策
   - 知识边界表（第1节）明确了每层职责

2. **关键约束有制约力**
   - D5（显式装配）+ D6（构造注入）锁死了反模式
   - D14（消费者定义接口）解开了许多循环依赖
   - D27–D28（include隔离 + 编译期校验）可通过工程手段兑现

3. **工程机制相对完整**
   - 测试分层清晰（第18节）：host单测 → Renode → HIL
   - CI矩阵（D64/D39）定义了验收标准
   - 中立性验收（T7a–T7c）是可执行的口径

4. **错误模型与事件语义已定**
   - D16–D18（事件 = 事实，禁指针）
   - D68（统一错误码）
   - D65（每生产者SPSC队列）无竞争

---

## 🔴 **关键瓶颈与缺陷**

### 1. **接口隔离的工程实现（D27最薄弱）**

**问题**：D27说"编译目标只暴露契约include路径"，但具体怎么做？

```cmake
# 现在的约定
app_dlt645只能include: app/dlt645 + edge_module + stdlib

# 但怎么防？
# ❌ CMake的target_include_directories没法"黑名单"
# ❌ 链接期符号隔离在C里无法实现（不像Rust或Go）
```

**后果**：
- app悄悄`#include "infra/flash/flash.h"`编译能过
- CI检查T7a依赖"gcc -M"解析，但在IAR下无同等工具
- 代码评审是最终防线，但不可自动化

**修复方向**：
```bash
# 建议补充方案：
1. 每个app编译时生成depend.txt（头部依赖集合）
2. CI强行检查："禁止集"中出现任何头
3. 违规编译拒绝（不仅warning）

# 对IAR：
# 需要解决方案，目前D27在IAR下漏洞
```

---

### 2. **sys_step()的打包边界不清（D52/D69混淆）**

**问题**：D52说"事件驱动+周期轮询混合"，D69说"每队列最多K个事件"。但没说清：

```c
sys_step()一次做什么？
├─ 从M个生产者队列各取K个事件？还是总共K个？
├─ 每个app的poll最多跑多久（毫秒还是指令数）？
├─ 超预算时是丢弃、跳过、还是告警后继续？
└─ 与告警计数的关系是什么？
```

**后果**：
- 实现时会有歧义，不同开发者打包方式不同
- 预算校验在集成阶段才能验证
- RTOS任务模型下，runner多步合并后预算失控的风险

**缺陷场景**：
```
poll1 (10us) + poll2 (10us) + poll3 (10us) = 30us ✓
poll4进来，预算告警（50us上限）
但poll4已入队，D52"协作式"无法打断
→ 实际执行 30+50 = 80us，超预算，但只是计数
→ "告警"成摆设
```

---

### 3. **PAL与嵌入式现实的鸿沟（D46/D85）**

**问题**：D46说"PAL只依赖极小原语"，但嵌入式的硬实时需求远超这五个函数。

```c
pal_critical_enter/exit()     /* 如果有中断嵌套呢？ */
pal_mb()                       /* ISR与runner间的栅栏 */
pal_now()                      /* 单调时间，但精度多少？ */
pal_in_isr()                   /* 上下文检测 */
pal_isr_enter/exit()           /* RTOS bridge */
```

**缺陷**：
- 临界区嵌套：cortex-m有4级中断优先级，D71的"原子序列"对高优先级ISR不保证
- 时间精度：D72用`(int32_t)(now - due) >= 0`防回绕，但如果tick是1ms而app需要100us粒度呢？
- RTOS bridge：D47说"框架不调RTOS API"，但`pal_isr_enter`正是在ISR里调，与不调的约束矛盾

**现实例子**：FreeRTOS的`portYIELD_FROM_ISR`需要比较当前ISR优先级与系统基线，这超出了D46的极小集

**修复方向**：
```c
/* D46应允许扩展，按使用场景分级 */
/* 核心（全必须）*/
pal_critical_enter/exit()
pal_now()

/* 高精度实时（可选） */
pal_timer_start_oneshot(us)
pal_timer_get_remaining_us()
pal_isr_priority_in_range()

/* 诊断（可选） */
pal_stack_check()
```

---

### 4. **app↔app间通信的"完全禁止"是幻想（D78/D79）**

**问题**：D78说"仅两条：事件+服务接口"，D79说"禁直接include对方"。但在实际产品中：

```c
/* app_meter和app_display都需要"当前费率"这个数据 */
/* 方案说：app_meter发事件，app_display订阅 */

/* 但问题来了：*/
1. 事件驱动天生延迟（至少一个sys_step周期）
2. app_display在poll里需要"当下"的值，不是"历史"
3. 解法只能是"服务接口"，但那违反D79
```

**实际代码的写法**：
```c
/* ❌ 违反D79但很诱人 */
#include "app_meter.h"
meter_data_t data = app_meter_get_now();  /* 直接调用 */

/* ✅ 遵守D78但很啰嗦 */
/* 在glue里写适配器 */
static edge_status_t ad_get(void *s, ...) {
    return app_meter_get_now();  /* 还是要知道app_meter_get_now的签名 */
}
/* app_display声明接口 */
typedef struct { edge_status_t (*get)(...); } meter_snapshot_if;
/* 最后发现：接口定义在app_display侧，但实现知道app_meter */
```

**后果**：
- 小规模产品（<10个app）会忍不住违反D79
- 大规模产品（50+个app）必须遵守，但增加了glue的复杂度
- 评审时的"这次就直接include一下"成为腐烂的开始

---

### 5. **sys与product的职责边界模糊（第5节C2-C3）**

**问题**：D5说product装配（显式列表），D2说sys决定顺序（优先级表）。但谁决定"哪些app是必需的"？

```c
/* main.c显式列表 */
edge_module_t *apps[] = { &dlt645, &dlms, &relay };
sys_run(sys_meter_family, apps, 3);

/* sys_meter_family的priority表里说relay的优先级是10 */
/* 但relay硬件不支持（板子没配继电器） */
/* 谁检查？产品编译时？sys启动时？ */
```

**现有做法**（D11备注）：
- 优先级表决定**顺序**
- 需要单独的"必需模块集合"做启动校验
- **但没说谁持有这个集合** ← 🔴关键漏洞

**后果**：
- 如果sys持有：意味着sys比product知道更多业务逻辑（违反D2）
- 如果product持有：就是另一份product/<name>/config.c或JSON（引入新概念）
- 都不持有：启动时只会发现缺的app已经开始运行了

---

### 6. **D45的认证边界在实施中的悖论**

**问题**：D45说"app在核心外，但不是运行期沙箱"。这意味着：

```c
认证边界 = 源码/变更控制 + ABI + 签名

/* 但三个产品各选不同的app组合 */
产品A：app_dlt645 + app_lcd
产品B：app_dlt645 + app_relay
产品C：app_relay + app_display

/* Q1: dlt645是否属于认证范围？ */
/* 如果是：每个产品都要单独认证，app复用失败 */
/* 如果否：app_dlt645独立认证，但它的错误可能导致产品B整体失效 */
```

**D34和D35都说"单独立项"，但没说怎么调和**

**现实场景**：
- 医疗设备法规要求"完整系统认证"，不认可"模块认证+组合"
- 计量法规要求每个产品单独送检
- 结果：D45的"核心认证可复用"在法规面前形同虚设

---

### 7. **event_id的号段分配是单点故障（D4/D31/D55）**

**问题**：中央分配表看似清晰，但在分布式开发中成为瓶颈。

```c
/* edge/events.h */
#define EDGE_EVT_UART0_RX    0x1001  /* 谁定的？ */
#define EDGE_EVT_RELAY_ON    0x2001  /* 冲突了怎么办？ */

/* 问题 */
1. 新增app_led时，需要申请0x3000段
2. 但events.h由哪个repo维护？main repo？app repo？
3. 假设有2个并行开发分支，各自分配0x3001..0x3010
4. merge时，冲突无法自动解决（不是文本冲突，是数值冲突）
5. CI的编译期校验（D28）能检测，但已经污染了代码
```

**与git的不兼容**：
- D28说"编译期检查"，但编译是在CI里
- 开发者本地编译通过了，远程CI才失败，体验差
- 多人并行开发新app的效率会显著下降

**改进方向**：
```c
/* 改为：module_id（分布式申请）+ event_offset（本模块内） */
EDGE_EVT(METER_DLT645, 1)  /* 0x0101 */
EDGE_EVT(METER_DLMS,   1)  /* 0x0201 */
EDGE_EVT(RELAY_GPIO,   1)  /* 0x0301 */

/* 每个app/<name>/有自己的module_id，不会冲突 */
```

---

### 8. **运行期隔离的细节没有（D53）**

**问题**：D53说"app失败后隔离"，但怎么隔离？

```c
运行期poll/on_event返回错误 → 计数 + 隔离

/* 隔离是指：*/
1. 永不再调这个app？
2. 还是记录一次失败，N次后才隔离？
3. 隔离后能否恢复？
4. 隔离期间的内存/资源怎么释放？
   （app可能持有buffer、打开的文件等）
5. 其它app依赖它的事件怎么办？
```

**现有文本**只说"停止调度、保留在列表"，没有状态机定义

---

## 🟡 **防腐与演进的薄弱处**

### 1. **ABI版本的演进约束（D40）**

**现状**：D40说"只增不改、绝不复用"，但没说**如何检查**。

```c
/* v1 */
struct edge_module_t {
    uint32_t id;
    poll_fn poll;
    on_event_fn on_event;
};

/* v2（增字段） */
struct edge_module_t {
    uint32_t id;
    poll_fn poll;
    on_event_fn on_event;
    suspend_fn suspend;  /* 新增 */
};

/* app_v1编译的.o文件 vs sys_v2的期望 */
/* 如果app在init时填充this->mod.suspend = NULL */
/* 字段偏移不变，仍兼容 */

/* 但如果 */
struct edge_module_t {
    uint32_t id;
    uint16_t version;   /* 新增在前面 */
    poll_fn poll;
    ...
};
/* 所有旧.o都坏了 */
```

**缺陷**：
- 只靠"开发纪律"防止，无编译期校验
- D28的"编译期检查"没有这一项
- 跨多个产品开发时，版本漂移的风险很高

---

### 2. **多RTOS的迁移成本（D50/D85改进后）**

**问题**：虽然D85把PAL分层了，但从裸机到某个RTOS的迁移成本未量化。

```c
裸机：
main() {
    board_init();
    while(1) sys_step();
}

RTOS（e.g. FreeRTOS）：
void runner_task(void *arg) {
    while(1) sys_step();
}
void isr_handler(void) {
    xQueueSendFromISR(...);
}

/* 需要改动的地方：*/
1. product/main.c - 需要创建任务、队列初始化
2. pal/cortex-m-freertos/ - ISR bridge
3. board的IRQ handler - 从直接push改为xQueueSendFromISR
4. CMakeLists.txt - link FreeRTOS
5. 新增product/runner_rtos.c（但D47说sys_step()的分解不代表app零改动）

/* 承诺："app零改动" */
/* 实际：product、board、PAL都要改，工作量至少3天 */
```

**与"中立性"的偏差**：
- 17.21矩阵说"换RTOS，只改product/main+PAL"
- 但board的IRQ handler也要改
- 而board是"标准库"层级，不应该因app而改

---

### 3. **时间精度的隐性约束（D20/D72）**

**问题**：D20注入clock_port，D72说比较用mod算术。但没说**时钟精度**是多少。

```c
D52: "模块声明period（tick；0 = 纯事件驱动）"

/* app_relay: period = 50（tick）*/
/* 如果1 tick = 1ms，则50ms */
/* 如果1 tick = 1us，则50us */

/* 承诺中立性，但 */
在某产品，tick = 1ms（board用10ms系统tick分频）
换产品时，tick = 1s（功耗很重要，用秒级tick）
← app_relay的period语义完全变了
```

**缺陷**：
- period没有单位定义
- D20的clock_port没说精度承诺
- 不同产品的timer精度差异被隐藏

---

### 4. **容错的不对称性**

**现状**：D53说init失败"默认跳过"。但：

```c
产品有10个app，某个app init失败
→ 生产部署前测出bug ✓ 好处

产品有10个app，某个app运行期失败3次被隔离
→ 生产环境，用户无感知 ✗ 坏处

不对称的策略会导致运维困难：
- 隔离意味着功能缺失
- 但无法远程诊断（app沉默了）
- D74的"诊断聚合"只有计数，无详细日志
```

---

## 🟢 **改进方向总结**

| 瓶颈 | 当前风险等级 | 修复优先级 | 建议 |
|---|---|---|---|
| include隔离（D27） | 🔴 高 | P1 | 补充IAR工具链支持；考虑编译指令级校验 |
| sys_step打包（D52/D69） | 🔴 高 | P1 | 明确define RUNNER_STEP_BUDGET_CYCLES/MS |
| PAL临界区嵌套（D46） | 🔴 高 | P2 | 扩展PAL允许可选的高精度原语 |
| app间通信二元论（D78） | 🟡 中 | P2 | 允许"只读共享状态"的特例，定义规范 |
| 必需模块集合（D11） | 🟡 中 | P2 | 新增edge_modules.yaml或CMake declare_required() |
| event_id分配（D4） | 🟡 中 | P2 | 改为分布式申请（每module_id号段） |
| 隔离细节（D53） | 🟡 中 | P3 | 补充状态机：OK→WARN→ISOLATED→可选RECOVER |
| ABI版本校验（D40） | 🟡 中 | P3 | 补充编译期layout断言脚本 |
| 时钟精度（D20/D72） | 🟡 中 | P3 | 明确clock_port的精度保证级别 |
| RTOS迁移成本（D50） | 🟢 低 | P4 | 补充迁移检查清单；提供脚本自动验证 |

---

## 最终判断

**完整性：7/10**
- 架构骨架完整 ✓
- 85条决策闭合 ✓
- 但工程实现细节有**8处关键漏洞**（含D27的IAR问题）

**可防腐性：5/10**
- 关键约束（D5/D6/D14）有制约力
- 但ABI版本、event_id冲突、多RTOS迁移的防腐机制缺失

**可操作性：6/10**
- 第1–17节清晰
- 第18节（测试）可执行
- 但D3的"board边界"、D52的"预算打包"在实施时会发现细节漏洞

**建议**：立项时补充P1问题的细化设计，尤其是D27（IAR include隔离工具链）和D52（runner预算定义），否则会在**集成阶段**付出代价。
